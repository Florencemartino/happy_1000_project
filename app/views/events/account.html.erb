
<%= render layout: "shared/main_layout", locals: {
  main_title: "C'est l'heure des comptes :",
} do %>

  <div class="card card-account card-account-body">
    <% average = @total/(@guests.count) %>
    <p><strong>Total : <%= @total %> € (<%= average.to_f %>€/ pers)</p></strong>

    <% @refund_per_person.each do |user, refunds| %>
    <div class="container-account">

      <div class="box-account">
          <% if user.photo.blank?%>
            <%= image_tag "default-user.png", alt: "alttext", class: "avatar avatar-account", id: "navbar-wagon-menu" %>
          <% else %>
            <%= cl_image_tag user.photo, height: 50, width: 50, crop: :fill %>
          <% end %>
          <p><%= user.nickname %></p>

      </div>

      <div class="box-account">
        <% refunds.each do |refund, amount| %>
         <p> <%= amount %>€ <br> -----------> </p>
      </div>

      <div class="box-account">
           <%= cl_image_tag user.photo, height: 50, width: 50, crop: :fill, class: "avatar avatar-account"%><%= refund.nickname %>
        <% end %>
      </div>

    </div>
    <% end %>



  </div>

  <div class="button-center" id-"terms">
  <%= link_to "Retour", event_dashboard_path(@event), :class => "btn btn-info mauve org-meal", false,:onclick=>'handleClick(this);' %>
  </div>

 <script>
  function handleClick()
   {
    if(document.getElementById('terms').checked)
     document.getElementById("Sign_up_id").disabled=true; // your submit button Id
    else
    document.getElementById("Sign_up_id").disabled=false;
   }
 </script>
<% end %>

