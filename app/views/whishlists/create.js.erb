// document.querySelector("[item='<%= @item.id %>']")
//   .remove();

function updateItem() {
  const itemBadge = document.getElementById("item-badge-<%= @item.id %>");
  itemBadge.classList.remove("d-none");
  itemBadge.innerHTML = "<%= @whishlist.quantity %>";
}

updateItem();
